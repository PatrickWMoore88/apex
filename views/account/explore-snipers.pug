extend account

append base
    br
    h3 Welcome to the Sniper Rifle Info
    br
    div.top
        each weapon in weapons
            -var weaponUrl = '/account/weapon-types/snipers/' + weapon.name
            -var imageUrl = weapon.imageURL
            .card-deck
                .card.text-white.bg-dark.mb-3(style='max-width: 18rem;')
                    img.card-img-top(src=imageUrl alt='Sniper Rifle Image')
                    .card-body
                    a.card-title(href=weaponUrl)= weapon.name