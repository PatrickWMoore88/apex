extend account

append base
    br
    h3 Welcome to the Pistol Info
    br
    div.explore-weapons
        each weapon in weapons
            -var weaponUrl = '/account/weapon-types/pistols/' + weapon.name
            -var imageUrl = weapon.imageURL
            .card-deck
                .card.text-white.bg-dark.mb-3(style='max-width: 18rem;')
                    img.card-img-top(src=imageUrl alt='Pistol Image')
                    .card-body
                    form(action=weaponUrl method='get')
                        button.btn.btn-primary(type='submit')= weapon.names